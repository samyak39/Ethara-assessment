# Used automatically by `vite build` (production builds on Vercel/Render).
# This is a public value (Vite bakes VITE_* vars into the client bundle), so it
# is safe to commit. Local `npm run dev` keeps using .env (localhost) instead.
VITE_API_URL=https://ethara-backend-2963.onrender.com/api
